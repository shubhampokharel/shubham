#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@tryghost/pretty-stream/node_modules/prettyjson/bin/prettyjson" "$@"
  ret=$?
else 
  node  "$basedir/../@tryghost/pretty-stream/node_modules/prettyjson/bin/prettyjson" "$@"
  ret=$?
fi
exit $ret
